# Supply-chain cooldown for pnpm: refuse any package version that was published
# less than 3 days ago (4320 minutes). pnpm computes the age at install time, so
# this is a true rolling window - no wrapper or cron needed. Add exceptions with
# minimum-release-age-exclude[]=<pkg> if a fresh release is ever required.
minimum-release-age=4320
